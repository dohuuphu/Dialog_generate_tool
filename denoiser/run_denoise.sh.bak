#!/bin/bash
echo "running...."
prex="speaker_4"
for i in {0..349}
    do 
        name="$i"
        # echo "make dir $name"
        # mkdir ./denoiser/$name
        python -m denoiser.enhance --dns64 --noisy_dir=./data/$name/ --out_dir=./result
        # echo "copy txt"
        # cp ./a/$name/*.txt ./result/$name/

    done
# python -m denoiser.denoiser.enhance --dns64 --noisy_dir=in --out_dir=out